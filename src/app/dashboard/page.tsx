import { Suspense } from 'react';

export default async function Page() {
    return (
        <main className=''>
            <h1 className={`mb-4 text-xl md:text-2xl`}>
                Dashboard
            </h1>
        </main>
    );
}